// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int fib(int n)
{
//     int f=1;
//     for(int i=1;i<=n;i++)
//     {
//         f=f*i;
//     }
// return f;


//recursion call for factorial
// if((n==1)||(n==0))
// {
//     return n;
// }

// return(n*factorial(n-1));

//recursion call for fibonacciseries


if(n==0)
{
    return 0;
}
else if(n==1)
{
    return 1;
}
return (fib(n-2)+fib(n-1));


}

int main() {
    
    int n;
    cin>>n;
    cout<<fib(n);
    
   

    return 0;
}
